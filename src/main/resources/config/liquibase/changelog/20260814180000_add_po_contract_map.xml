<?xml version="1.0" encoding="utf-8"?>
<databaseChangeLog
    xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-latest.xsd">

    <!--
        Maps a purchase order's unversioned number (ponumber_base, e.g. PO601566) to
        contract numbers extracted from non-standard purchase_order.contractnumber values
        (slash, brace, colon, space, etc.). One PO may map to several contract numbers.
    -->
    <changeSet id="20260814180000-1" author="dcpass">
        <createTable tableName="po_contract_map">
            <column name="ponumber_base" type="varchar(255)">
                <constraints primaryKey="true" nullable="false"/>
            </column>
            <column name="contractnumber" type="varchar(255)">
                <constraints primaryKey="true" nullable="false"/>
            </column>
        </createTable>
        <createIndex indexName="idx_po_contract_map_contractnumber" tableName="po_contract_map">
            <column name="contractnumber"/>
        </createIndex>
    </changeSet>

    <!--
        Rebuild mapping from messy purchase_order.contractnumber values (slash, brace,
        colon, space, parentheses). Extracts CW ids, DC agency numbers, C-numbers,
        GSA schedules, and slash-separated compact ids. Safe to re-run after a data reload.
    -->
    <changeSet id="20260814180000-2" author="dcpass" dbms="postgresql">
        <sql splitStatements="false"><![CDATA[
            CREATE OR REPLACE FUNCTION refresh_po_contract_map() RETURNS integer
            LANGUAGE plpgsql AS $fn$
            DECLARE
                inserted integer;
            BEGIN
                TRUNCATE po_contract_map;

                INSERT INTO po_contract_map (ponumber_base, contractnumber)
                WITH messy AS (
                    SELECT DISTINCT ponumber_base, contractnumber AS source
                    FROM purchase_order
                    WHERE contractnumber ~ '[/{}: ()]'
                      AND ponumber_base IS NOT NULL
                      AND ponumber_base <> ''
                      AND btrim(contractnumber) <> ''
                ),
                regex_hits AS (
                    SELECT m.ponumber_base, upper(x[1]) AS token
                    FROM messy m,
                    LATERAL regexp_matches(
                        m.source,
                        '(?i)(CW[0-9]{4,8}|[A-Z0-9]{2,10}-[0-9]{2,4}(?:-[A-Z0-9]+)+|GS-[0-9]{1,3}[A-Z]?-[0-9A-Z]+|\mC[0-9]{3,6}(?:-V[0-9]+)?\M|[0-9]{2}[A-Z]{4}[0-9]{2}[A-Z][0-9A-Z]{3,}|\m[0-9]{4}-[0-9]{1,3}\M)',
                        'gi'
                    ) AS x
                ),
                slash_parts AS (
                    SELECT
                        m.ponumber_base,
                        trim(both ' -_.,;:#+*' FROM
                            CASE
                                WHEN btrim(part) ~ '\s'
                                    THEN split_part(regexp_replace(btrim(part), '\s+', ' ', 'g'), ' ', 1)
                                ELSE btrim(part)
                            END
                        ) AS token
                    FROM messy m,
                    LATERAL unnest(regexp_split_to_array(
                        regexp_replace(m.source, '\([^)]*\)', ' ', 'g'),
                        '/'
                    )) AS part
                )
                SELECT DISTINCT ponumber_base, upper(token)
                FROM (
                    SELECT ponumber_base, token FROM regex_hits
                    UNION ALL
                    SELECT ponumber_base, token FROM slash_parts
                ) u
                WHERE token ~ '[0-9]'
                  AND length(token) BETWEEN 4 AND 40
                  AND token ~ '^[A-Z0-9]([A-Z0-9._-]*[A-Z0-9])?$'
                  AND token !~ '^(19|20)[0-9]{2}$'
                  AND token !~ '^(M[0-9]{1,5}|TO[0-9]{1,3}|NA|NONE|NULL|N/A|FY[0-9]{2,4})$';

                GET DIAGNOSTICS inserted = ROW_COUNT;
                RETURN inserted;
            END;
            $fn$;
        ]]></sql>
        <rollback>
            <sql>DROP FUNCTION IF EXISTS refresh_po_contract_map();</sql>
        </rollback>
    </changeSet>

    <changeSet id="20260814180000-3" author="dcpass" dbms="postgresql">
        <sql>SELECT refresh_po_contract_map();</sql>
        <rollback>
            <sql>TRUNCATE po_contract_map;</sql>
        </rollback>
    </changeSet>
</databaseChangeLog>
